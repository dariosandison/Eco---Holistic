import React from 'react';

/**
 * Minimal MDX components map.
 * We export the named `mdxComponents` to satisfy imports like:
 *   import { mdxComponents } from '../components/MDXComponents'
 *
 * Leaving it empty preserves your existing MDX content exactly as-is,
 * without altering rendering. You can add custom mappings here later.
 */
const mdxComponents = {};

export default mdxComponents;
export { mdxComponents };
