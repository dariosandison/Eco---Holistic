{
  "name": "wild-and-well",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": "18.x" },
  "scripts": {
    "dev": "next dev",
    "build": "next build && node scripts/generate-sitemap.mjs",
    "start": "next start"
  },
  "dependencies": {
    // keep your existing deps here
  }
}
